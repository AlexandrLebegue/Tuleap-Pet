node_modules
dist
out
.DS_Store
.eslintcache
*.log*
ressource/*

# CI integration tests (Tuleap dockerisé) - secrets et état local
.tuleap-test.env

# Chromium headless téléchargé par puppeteer (embarqué au build via extraResources)
.puppeteer-cache